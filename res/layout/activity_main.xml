<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res/net.microtrash.slicedup"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    tools:context=".MainActivity" >

    <SurfaceView
        android:id="@+id/preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <net.microtrash.slicedup.view.PreviewMask
        android:id="@+id/mask"
        android:layout_width="match_parent"
        android:layout_height="match_parent" >

        <RelativeLayout
            android:id="@+id/mask_top"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/gui_main_color" >
        </RelativeLayout>

        <RelativeLayout
            android:id="@+id/mask_bottom"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_alignParentBottom="true"
            android:background="@color/gui_main_color" >
        </RelativeLayout>

        <ImageView
            android:id="@+id/outline"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:alpha="0.5"
            android:src="@drawable/figure"
            android:contentDescription="@null" />
    </net.microtrash.slicedup.view.PreviewMask>

    <View
        android:id="@+id/shutter_layer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#AAFFFFFF"
        android:visibility="gone" />

    <net.microtrash.slicedup.view.IconButton
        android:id="@+id/bt_shoot"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="-10dp"
        app:circleColor="#113847"
        app:circleRadius="40dp"
        app:icon="@drawable/camera" />

</RelativeLayout>